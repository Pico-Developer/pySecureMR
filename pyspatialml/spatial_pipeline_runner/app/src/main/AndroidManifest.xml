<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="com.picovr.permission.SPATIAL_DATA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PySpatialMLSpatialRunner">

        <meta-data
            android:name="pico.spatial.use_experimental_api"
            android:value="1" />
        <meta-data
            android:name="pvr.app.secure_mr"
            android:value="1" />
        <meta-data
            android:name="pvr.app.type"
            android:value="vr" />
        <meta-data
            android:name="enable_mr_safeguard"
            android:value="1" />

        <activity
            android:name="com.bytedance.pico.pyspatialml.spatialrunner.MainActivity"
            android:configChanges="screenLayout|screenSize|smallestScreenSize|orientation"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="pico.spatial.windowcontainer.id"
                android:value="pySpatialMLSpatialRunner" />
            <meta-data
                android:name="pico.spatial.windowcontainer.resizetype"
                android:value="2" />
            <meta-data
                android:name="pico.spatial.windowcontainer.style"
                android:value="1" />
            <meta-data
                android:name="pico.spatial.windowcontainer.defaultsize"
                android:value="640x360" />
            <meta-data
                android:name="pico.spatial.windowcontainer.materialbackground"
                android:value="0" />
        </activity>
    </application>
</manifest>
