
## Implement transpose operator

There is no `transpose` operator inside securemr operator, but it can be implemented by `arithmetic`:
"T({0} / 2)" in `arithmetic` means divide first tensor and then transpose it.
